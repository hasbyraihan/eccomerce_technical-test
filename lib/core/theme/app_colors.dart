import 'package:flutter/material.dart';

/// Semua warna global disimpan di sini supaya konsisten
class AppColors {
  static const primary = Color.fromARGB(255, 247, 177, 72);
  static const secondary = Color.fromARGB(255, 247, 171, 58);
  static const background = Colors.white;
  static const textPrimary = Colors.black;
  static const accent = Color.fromARGB(255, 255, 149, 0);
}
